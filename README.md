## emm 

使用ts 编写
react组件

可以被react应用引用
还是分离的 like import Button from '@material-ui/core/Button';

用webpack打的包

做一个组件库
mocha & ts & react & webpack

npx webpack
npx是啥来着？

安装webpack-cli
npm install webpack webpack-cli --save-dev
npm i typescript --save-dev
npm i ts-loader --save-dev

tsconfig.json

tsx/jsx 编译成什么了？
貌似是这种 形容组件的
//React.createElement("div")
jsx to js
从react项目里扒的 //处理tsx & jsx
npm i babel-loader babel-preset-react-app babel-preset-react-app babel-plugin-named-asset-import --save-dev

写一个示例组件

在一个js react项目里引用试试
npm install --save-dev jest
npm i --save-dev @types/jest
npm i @testing-library/react --save-dev
react-dom
